---
- name: Install MySQL
  hosts: lamp
  become: true
  tasks:
    - name: Install MySQL Server
      ansible.builtin.apt:
        name: mysql-server
        state: latest
    - name: Install MySQL Client
      ansible.builtin.apt:
        name: mysql-client
        state: latest
