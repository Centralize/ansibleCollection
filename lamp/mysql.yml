---
- name: Install MySQL
  hosts: lamp
  become: true
  tasks:
    - name: Install MySQL Server
      ansible.builtin.apt:
        name: mariadb-server
        state: latest
    - name: Install MySQL Client
      ansible.builtin.apt:
        name: mariadb-client
        state: latest
